# This workflow will build a Swift project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-swift

name: Unit Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  build:

    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Build
      run: swift build -v
      
    - name: Run Tests with Coverage
      run: swift test -v --parallel --enable-code-coverage

    - name: Export Coverage Report
      run: |
        BIN_PATH=$(swift build --show-bin-path)
        PROFDATA_PATH="${BIN_PATH}/codecov/default.profdata"
        [ ! -f "${PROFDATA_PATH}" ] && { echo "Error: Expected ${PROFDATA_PATH} to exist"; exit 1; }
        XCTEST_BUNDLE="${BIN_PATH}/OrTests.xctest"
        [ ! -d "${XCTEST_BUNDLE}" ] && { echo "Error: Expected ${XCTEST_BUNDLE} to exist"; exit 1; }
        XCTEST_NAME="OrTests"
        xcrun llvm-cov export \
          --format=lcov \
          --instr-profile="${PROFDATA_PATH}" \
          --ignore-filename-regex="\\.build|Tests" \
          "${XCTEST_BUNDLE}/Contents/MacOS/${XCTEST_NAME}" \
          > coverage.lcov

    - name: Upload Coverage to Codecov
      uses: codecov/codecov-action@v5
      with:
        files: coverage.lcov
        fail_ci_if_error: false
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

    - name: Test Summary
      run: |
        echo "✅ All tests completed successfully!"
      if: success()
      
    - name: Test Failure Report
      run: |
        echo "❌ Tests failed. Please check the test output above for details."
      if: failure()
